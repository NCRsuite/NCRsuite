# Installation V2.6.3

## 1. Supabase

Exécuter uniquement :

`supabase/migrations/040_security_shift_end_gps_permissions_fix.sql`

Cette migration remplace la fonction de prise/fin de poste sans modifier son interface publique.

## 2. GitHub

Déposer tout le contenu du patch à la racine de la V2.6.2.

## 3. Cloudflare

Attendre la fin du build, fermer la PWA puis la rouvrir.

## Contrôles

- Une vacation déjà prise peut être terminée même si son heure de fin est dépassée depuis plus de huit heures.
- Une main courante partiellement fermée est régularisée puis clôturée proprement.
- Le message d’erreur réel est affiché en cas de refus serveur.
- Le guide GPS mentionne le navigateur réellement utilisé sur iPhone et permet de retester la position.
